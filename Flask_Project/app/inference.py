import os


def mlmodel(image_url):
    numpber_plate_text="HR73474732HH"
    return numpber_plate_text


def predict(image_url):
    result_text=mlmodel(image_url)
    return {"result_text":result_text}